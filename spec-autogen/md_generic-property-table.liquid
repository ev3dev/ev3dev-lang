
Property Name|Type|Accessibility|Description
---|---|---|---
{% for prop in currentClass.systemProperties
    %}{{prop.name}}|{{prop.type}}|{{ '(readAccess ? "Read" : "") + (readAccess && writeAccess ? "/" : "") + (writeAccess ? "Write" : "")' | eval: prop }}|{% for line in prop.description %} {{line}}{% endfor %}
{% endfor %}
